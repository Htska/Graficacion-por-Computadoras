# openGL3
OpenGL code for drawing multiple figures inside another using the Fragment shader.

# Execution
To compile:
```bash
cmake -B build
cmake --build build
```
To run:
```bash
./build/opengl
```
